import React from 'react';

const appName = "Motsune";

export class AppHeader extends React.Component {
    render() {
        return (
            <h1>
                { appName }
            </h1>
        );
    };
};